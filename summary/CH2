# 깊은 복사에 대해

파이썬에서 튜플, 문자열, 바이트는 불변 / 리스트와 바이트는 가변 객체
깊은 복사를 하면 변환이 되어도 영향을 받지 않지만, 얕은 복사는 영향을 받는다.

# 문자열에 대해서

파이썬은 불변의 str 타입을 사용해 문자열을 표현한다.
문자열 메서드는 join, l/rjust, format, 문자열 언패킹 (**locals()), splitline, split, strip()
문자열의 위치를 찾는 메서드는 (r)index, find, replace 등이 존재한다.

# 튜플에 대해서

튜플은 쉼표로 구분된 값으로 이루어지는 불변 시퀀스 타입이다.
튜플 메서드는 count, index, 언패킹 (x, *y) 등이 있다.
collection의 기능 중 네임드 튜플이라는 이름으로 항목의 이름 참조가 가능하다.
첫번째 인수는 튜플의 이름, 두번째 인수는 공백으로 구분된 문자열이다.

# 리스트에 대해서

파이썬에서 배열과 유사한 (가변) 객체로, 동적으로 크기 조절이 가능하다.
(참고로 연결 리스트는 추상 데이터 타입이다)
파이썬의 메소드는 append, pop (O(1)) 이고 remove, index, in, insert는 위치 확인 및 삽입 (O(n))
튜플과 유사한 형태로 리스트도 리스트 언패킹 (x, *y)가 가능하다.
리스트 메서드의 성능을 측정할 때는 timeit 모듈의 Timer 객체를 사용한다.

# 바이트와 바이트 배열

파이썬에서 원시 바이트를 처리하는데 쓰는 불변 타입이다.
비트와 비트 연산자를 통해 곱셈을 하거나 특정 조건의 확인이 가능하다.
